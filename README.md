# Thai Island

Prerequisites: python 3.x

Install virtual environment with dependencies:
```bash
$ python3 -m venv venv
. venv/bin/activate
pip3 install Flask
```

To run:
```bash
$ export FLASK_APP=thai_island 
``` 
